import React from "react";
import styles from "./style.module.scss";

const PageLogin = () => (
  <div className={styles.wrapper}>This is login page</div>
);

export default PageLogin;

PageLogin.propTypes = {};
